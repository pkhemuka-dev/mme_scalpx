# Batch 29BF Runbook

This is a diagnostic-only Lane-B batch. It identifies whether features/decisions movement is caused by running features/strategy services, generic main processes, systemd collision, delayed tail writes, or an unproven hidden publisher.

No patch, no service stop/start, no Redis restart, no paper/live, no orders.
