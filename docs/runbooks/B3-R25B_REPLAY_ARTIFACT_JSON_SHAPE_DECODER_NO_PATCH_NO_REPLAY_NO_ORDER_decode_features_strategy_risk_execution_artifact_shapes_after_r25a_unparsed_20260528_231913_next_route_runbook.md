# B3-R25B_REPLAY_ARTIFACT_JSON_SHAPE_DECODER_NO_PATCH_NO_REPLAY_NO_ORDER next route

If classification is PASS:

Run B3-R25C with the discovered row paths.

Goal:

1. Extract actual rows from `features_rows.json` and `strategy_decisions.json`.
2. Count actions / HOLD / candidates / blockers.
3. Confirm whether economics fields are absent or nested.
4. No patch until this is proven.
