# Batch 30J-R5H Runbook

Purpose:
Inspect `03_integrity_report.json` after R5G found a `fail` token.

Proof:
`run/proofs/proof_batch30j_r5h_integrity_report_fail_token_classifier_latest.json`

Next:
Do not proceed to parity. Inspect exact true_blocker_refs and repair/prove replay integrity source.
