# 30J-R5BQ-R3 Runbook

Purpose: patch explicit replay integrity stem equivalence normalization using the exact R5BQ-R2A regex anchor.

This batch does not run replay.

Next required batch: R5BR no-execution fixture validation.

Proof: `run/proofs/proof_batch30j_r5bq_r3_exact_regex_anchor_patch_latest.json`
