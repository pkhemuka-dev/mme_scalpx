# Batch 29BP-R1 Runbook

This batch waits for short-TTL stale runtime locks to expire naturally and proves runtime cleanliness.

Proof:
run/proofs/proof_batch29bp_r1_passive_stale_lock_expiry_latest.json

Next:
Do not run replay. Run guarded pfeeds/pfeedcheck source inspection and cleanup batch.
