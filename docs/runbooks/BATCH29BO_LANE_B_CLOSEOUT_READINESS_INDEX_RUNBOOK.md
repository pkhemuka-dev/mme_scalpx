# Batch 29BO Runbook

Lane-B closeout readiness index. This is proof-only and does not stop feeds.

Use this as the final Lane-B index for the session only if verdict is PASS_GREEN_CONTINUE.

Lane A still owns controlled-paper readiness and strategy-validity issues.
Lane C still owns replay/live parity and dataset materialization.
Run a separate explicit pstopday/stop command only when the operator decides the live observation window is over.
