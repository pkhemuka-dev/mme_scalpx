# Batch 26-O3 — Provider Runtime Publication

Publishes/verifies `state:provider:runtime`.

This batch is fail-closed:
- no paper approval
- no real live approval
- no risk/execution start
- provider readiness is true only if statuses are actually ready
- otherwise the hash exists with UNAVAILABLE statuses and blocked reason
