# B1-PROFIT-LIVE-R37M-R1_INTEGRATE_RECORDER_STATUS_INTO_PCHECK_NO_ORDER runbook

Use one dashboard:

```bash
source ~/.bash_aliases
pcheck
```

`pcheck5min` remains available but simply calls `pcheck`.
