# B3-R61C next route

If PASS, run B3-R61D normalized dataset build/replay smoke.

Normalization should add `ts_event` and `symbol` to generated fut_ticks/opt_ticks rows.
No source patch required yet; this can be dataset-build normalization only.
