# PDASH-R2_PLAIN_LANGUAGE_BLOCKER_CONTRACT_WATCH_READONLY_NO_START_NO_ORDER_add_simple_blocker_contract_watch_panel_20260710_004016 runbook

Manual UI start was not performed by this batch.

Later manual start only, after PASS:

```bash
cd /home/Lenovo/scalpx/projects/mme_scalpx
export PYTHONPATH="$PWD:${PYTHONPATH:-}"
bin/pdash_lite --host 127.0.0.1 --port 8787
```

Open:

- http://127.0.0.1:8787/
- http://127.0.0.1:8787/json
