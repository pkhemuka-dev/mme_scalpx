# Batch 30F-R8 Runbook

Post-clean Lane-C recheck after generic owner cleanup. Confirms `lock:execution` is absent and reselects the dataset candidate.

If GREEN_CONTINUE, proceed to Batch 30G selected dataset structure validator, offline only.
