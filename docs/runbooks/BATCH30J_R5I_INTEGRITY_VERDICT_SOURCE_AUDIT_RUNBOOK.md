# Batch 30J-R5I Runbook

Purpose:
Audit source of replay integrity verdict after R5H confirmed `verdict=fail` with zero failed checks.

Proof:
`run/proofs/proof_batch30j_r5i_integrity_verdict_source_audit_latest.json`

Backups:
`run/_code_backups/batch30j_r5i_integrity_verdict_source_audit_20260508_144016`

Next:
Write minimal patch batch for integrity verdict aggregation only, then rerun guarded replay.
