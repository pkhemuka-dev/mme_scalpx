# B3-R54A next route

If root cause is confirmed, run B3-R55 patch-plan only.

Likely fix: helper should select artifact dir/file set based on presence of candidate_audit with non-zero rows, not first economics_summary parent only.
