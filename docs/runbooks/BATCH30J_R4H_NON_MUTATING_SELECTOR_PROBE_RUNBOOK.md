# Batch 30J-R4H Runbook

Non-mutating selector readability probe for candidate available_dates generated by 30J-R4G.

This batch imports replay selector modules and performs static/dynamic probe attempts only. It does not run replay_run.py, does not install metadata into dataset root, and does not start services.

If GREEN_CONTINUE, proceed to reversible metadata install planning/dry selector validation. If REVIEW_REQUIRED with static-only compatibility, perform source-specific selector adapter validation before install.
