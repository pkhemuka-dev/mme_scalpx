# Redis reference files

These files were copied from the old VM as reference material only:

- common/bin/redis_tls_cli.sh
- common/etc/redis_cluster.env
- common/lib/redis_conn.py

They represent the old TLS remote Redis cluster client pattern and are not yet wired into the current mme_scalpx runtime.
Current mme_scalpx dev mode uses local Redis.
